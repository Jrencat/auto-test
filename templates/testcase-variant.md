# VARIANT 数据变体矩阵用例 —— <模块名>

> 多分支页面专用：同一页面因**判别字段**（如类型 type / 分类 category）取值不同，渲染不同列/表单/按钮/接口。
> 由 auto-test Skill 增量维护。ID 一经分配不得复用/重排；终态只允许真实执行结果。
> 判别字段与取值枚举来自源码分支（见 source-analysis-rule §1.5），不是猜测；驱动标识执行前动态取号（§1.6）。
> 脚本目录来源：`<cwd>/.claude/auto-test/project.json`（下例为通用示例）。

## 判别维度

- **主判别字段**：<字段名，来源：查询后由后端返回决定 / 用户手选>
- **取值枚举**（抄源码 if/else 分支）：<type1 / type2 / type3 / …>
- **次级维度**（正交叠加）：<操作阶段 flag / 状态 status / 受控标志 等>

## 变体矩阵（每行 = 至少一条用例 + 一条参数化脚本用例）

| ID | 页面 | 判别取值 | 驱动标识(动态取号) | 期望专属列/表单 | 显隐/必填/按钮/接口差异 | 实际渲染(回填) | 状态 | 证据 |
|----|------|---------|------------------|----------------|------------------------|---------------|------|------|
| ORDER-LIST-001 | 列表 | type1 | <动态取号> | <专属列/字段> | <差异> | <执行后回填真实表头> | 🟡→回写 | <截图/trace 路径> |
| … | | | | | | | | |

**对应脚本**：`<frontendTestsDir>/e2e/<module>.<page>-variant.e2e.spec.ts`（参数化，见 script-rule §变体矩阵骨架）
**执行命令**：`npx playwright test <module>.<page>-variant --project=e2e`

## 覆盖完整性声明

- 判别字段共识别出 **N** 个取值；本轮真实执行 **M** 个（✅PASS x / ⚠BLOCKED y）。
- 未覆盖取值及原因：<数据空档 / 标识过期 / 时间未及扩展>，逐项列明——严禁因"其它取值已验证"就当作已覆盖。
